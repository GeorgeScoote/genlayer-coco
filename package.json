{
  "name": "genlayer-consensus-game",
  "version": "1.0.0",
  "description": "Decentralized word-matching game on GenLayer with AI semantic analysis",
  "scripts": {
    "dev": "npx serve frontend/public -p 3000",
    "deploy": "bash scripts/deploy.sh",
    "test": "pytest tests/ -v"
  },
  "keywords": ["genlayer", "blockchain", "ai", "web3", "game"],
  "author": "GenLayer Community",
  "license": "MIT",
  "devDependencies": {
    "serve": "^14.2.0"
  },
  "engines": {
    "node": ">=18.0.0"
  }
}
